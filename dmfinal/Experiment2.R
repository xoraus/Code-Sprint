# Todo for Experminet 2

# 1. SELECT a dataset with categorical featres,  convert it into numeric
# 2. an o/p & remove it from processing
# 3.a feature missing values & set a default vale

# T_b <- as.factor(c("vc",""))
# t_B
# unclass(tb)
clrs <- factor(c("red", "green", "blue"))
clrs
as.integer(clrs)
